defmodule Shepherd do
  @moduledoc """
  Elixir Burrito TUI for the weftspun agent fleet: status and workspace
  gates. The enrol / rotate / migrate / revoke ceremony was trimmed
  after per-agent `BAO_TOKEN` + `bao login -no-store` closed the
  stale-token failure mode from RFD 2195 DETAILS.
  """
end
